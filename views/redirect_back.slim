.alert.alert-info
  == I18n.t(:countdown, {time: '<span id="CDT"></span>', goback: %{<a href="#{@url}" id="referer">#{I18n.t(:goback)}</a>}})
javascript:
  timeCount = 10
  function countDown() {
    document.getElementById('CDT').innerHTML = timeCount
    if (timeCount-- > 0) {
      setTimeout('countDown()', 1000);
    } else {
      location.href = document.getElementById('referer').getAttribute('href');
    }
  }
  countDown();
